/-!
# Covariant Tensor Riemannian Pointwise-Inner Layer Placeholder

This module name is preserved for the vendored tensor tree, but its upstream
contents depend on the pointwise-inner/measure layer that RicciFlower is
intentionally not importing yet.  The usable foundation for now is
`RicciFlower.Tensor.RSTensor.Defs`, `Field`, and `Metric`.
-/

