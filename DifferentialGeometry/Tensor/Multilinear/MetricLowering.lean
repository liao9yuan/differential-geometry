import DifferentialGeometry.Geometry.Metric.PointwiseInner.MetricLowering

/-!
# Compatibility re-export for metric lowering of multilinear fields

The metric-dependent implementation lives in the geometry hierarchy; this module preserves the
former tensor import path.
-/
