import RicciFlower.Tensor.Multilinear.BundleSmoothEval

/-!
# Compatibility wrapper for smooth evaluation on multilinear bundles

This legacy path re-exports the RicciFlower module so the combined import graph
does not load two copies of the same multilinear smooth-evaluation API.
-/
