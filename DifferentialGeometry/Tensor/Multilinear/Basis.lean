import RicciFlower.Tensor.Multilinear.Basis

/-!
# Compatibility wrapper for multilinear bases

The active mixed import graph uses the RicciFlower tensor tree as canonical.
This legacy path re-exports the RicciFlower module to avoid duplicate global
tensor declarations.
-/
