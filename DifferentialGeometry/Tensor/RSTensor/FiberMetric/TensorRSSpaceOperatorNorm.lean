import DifferentialGeometry.Tensor.RSTensor.TensorRSSpaceOperatorNorm

/-!
# Compatibility re-export for tensor-space operator norms

The generalized implementation remains at the established tensor API path; this module preserves
the former fibre-metric import path.
-/
