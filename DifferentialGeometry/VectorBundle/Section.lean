import RicciFlower.VectorBundle.Section

/-!
# Compatibility wrapper for smooth vector-bundle sections

The RicciFlower vector-bundle section module is the canonical copy in the mixed
RicciFlower/measure environment.  This legacy `DifferentialGeometry.*` import path
re-exports it to avoid duplicate global declarations such as
`ContMDiffVectorBundleHom.mapSection`.
-/
