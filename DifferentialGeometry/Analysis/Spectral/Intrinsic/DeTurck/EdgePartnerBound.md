# EdgePartnerBound

## Current source state

`EdgePartnerBound.lean` contains no `sorry`, `admit`, axiom, or opaque
replacement.  Its complete source now passes a focused Lean check with no local
warning, and its exact target is GREEN.  Direct audits of `edgeFull_one`,
`edgeSlot_one`, `edgeRaise_gen`, `edgePartner_gen`, `edgeTop_zero`, and
`edgeTop_one` report exactly
`[propext, Classical.choice, Quot.sound]`, with no `sorryAx`.  Downstream
modules may now consume the exact-current artifact.

The source-level public producers are:

- `edgeFull_one` and `edgeFull_zero`, controlling a full relative inverse
  insertion and its first background covariant derivative;
- `edgeSlot_zero` and `edgeSlot_one`, giving the exact one-slot product rule
  without an artificial zeroth-order derivative term;
- `edgeRaise_gen` and `edgeRaise_bds`, controlling both inverse insertions;
- `edgePartner_gen` and `edgePartner_one`, proving the sharp pointwise estimate
  `|nabla P_sigma|_g^2 <= C delta^2 |nabla T|_g^2` for one explicit formal
  partner; and
- `edgePartner_zero`, proving the companion zero-order estimate
  `|P_sigma|_g^2 <= C delta^2 |T|_g^2`.
- `edgeBi_zero_unif`, choosing the polarized zero-order constant before the
  reference metric varies; `edgePartnerBi_zero` is its metricwise compatibility
  projection.
- `edgeTopBi_zero_unif`, assembling the complete raw `qA/qB/q/epsilon`
  polarized top partner at fixed path parameter.  Its constant is chosen before
  `g`, and its norm-level conclusion is
  `fiberLp(topPartnerBi) <= K * fiberLp(P) * fiberLp(V)` with no symmetry,
  fibre-radius, or Sobolev-radius condition on `P` or `V`.
- `edgePartnerBi_zero` and `edgePartnerBi_one`, retaining the genuinely
  polarized products
  `|P|^2 |V|^2` and
  `|P|^2 |nabla V|^2 + |nabla P|^2 |V|^2
  + |nabla W|^2 |P|^2 |V|^2`.  The moving-path perturbation `W`, the raised
  coefficient state `P`, and the test tensor `V` are separate.  Only `W` is
  assumed to lie in the half fibre ball; neither `P` nor `V` has a fibre or
  Sobolev radius hypothesis, and `P` needs no symmetry hypothesis.
- `edgeTop_zero` and `edgeTop_one`, assembling the zero- and one-derivative estimates for the complete
  Riemann--DeTurck formal partner returned by `edgeTop_green`, uniformly for
  every `s` in the realized path `g + s T`.

The generalized statements distinguish the path perturbation `P` from the
partner tensor `T`.  This distinction is essential on the realized segment:
the moving inverse coefficient is generated by `P = s T`, while the formal
partner still contains the unscaled difference `T`.  The proof uses
`|nabla(s T)|^2 <= |nabla T|^2` for `s in [0,1]` and never replaces the
coefficient derivative by a generic high-jet envelope.

The corrected arbitrary-state polarized bounds, including the class-first
zero-order projection, pass the focused source check.
The complete fixed-parameter polarized top assembly uses the same finite
`52`-factor bookkeeping as `edgeTop_zero`, applied to `edgeBi_zero_unif`, and
then takes one square root.  It is a routine consumer of the polarized
monomial estimate, not the joint analytic `B02` cancellation frontier.
They are the local estimate needed by `edgePair_green_bi` when its independent
test field is not the metric deviation.  The arbitrary `P` form is essential
for the paired Green swap, where the raised state is instantiated both by `T`
and by `L T`; no smallness assumption on `L T` is available or needed.  In
particular, these bounds introduce neither a fourth metric jet nor an `H4`/`H5`
state radius.  They do not by themselves prove the full low-base Rung-3
residual: that consumer must first keep the order-zero and top path pieces in
the joint Riemann--Palatini-cancelled refold, rather than estimating the
order-zero coefficient separately.

## Verification repair

The first conclusive focused check exposed source/API drift rather than a
mathematical obstruction.  The file was missing the direct
`MetricRealization` and realized-family namespace openings, and it referred to
an upstream private identity for `gInvRaisedEndo g g`.  The latter is now
reproved locally from `gInvRaisedEndo_apply` and
`inverseMetricSharpFib_g0FlatCLM`.

The apparent heartbeat wall in `edgeRaise_gen` was caused by four uses of
`add_le_add_right` in the wrong syntactic orientation.  On the large tensor
goal this forced repeated `whnf`; replacing them with explicit
`add_le_add ... le_rfl` made the complete file check quickly.  Section-valued
scalar multiples also had to be evaluated through
`ContMDiffSection.coe_smul` before applying the fibre-norm scaling theorem.
The final source contains no `whnf`.

## Mathematical role

Together with `EdgeRefoldPairing.edgeTop_green`, the differentiated partner
bound is the sharp top-arm cancellation needed at the closed edge.  Every
Leibniz term retains one undifferentiated metric difference, producing the
small factor `delta`; there is no additive source and no coefficient depending
on uncontrolled derivatives of an arbitrary endpoint solution.

The public, sorry-free theorem
`exists_iteratedCovGrad_covDivergence_l2_le` is available in
`CovDivergenceRoughLaplacianCommutation.lean`.  A sharper pointwise contraction
estimate `|div P|^2 <= dim(E) |nabla P|^2` is currently only private in
`DirichletSpectralBochnerGap.lean`.  `EdgeRateBound.edgeTop_pair_le` already
uses the public coarse L2 estimate together with both verified source bounds;
after the exact refresh here, its focused verification is the next packaging
step.

## Exact remaining uniqueness bridge

This file removes only the top formal-partner derivative obstruction.  A full
closed-edge Ricci--DeTurck rate estimate still needs the joint visible-arm
pairing for `edgeRicciHalf`, `edgeFold0`, and `edgeQuad1`, followed by
`edgePrincipal_half`, `movingEnergy_zero`, and the existing interior
continuation.  Even a complete Ricci--DeTurck uniqueness theorem does not by
itself prove uniqueness of the original Ricci flow: the harmonic-map/common
DeTurck gauge must still be constructed with its existence, regularity, gauge
PDE identity, and inverse-flow undoing.

## Honest progress

- Exact `ricci_flow_forward_unique`: complete and axiom-clean in the current
  post-merge baseline; it is no longer part of this frontier.
- This partner-bound producer: **100% as stated**, focused-GREEN,
  exact-current, and axiom-clean.
- Closed-edge Ricci--DeTurck energy machinery: materially advanced at the top
  arm, but the joint visible lower-arm estimate and final rate packaging remain.
- `extends_of_rmBounded` and the Hamilton positive-Ricci program are unchanged
  until both requested analytic endpoints are axiom-clean.
