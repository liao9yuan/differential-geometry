# DeTurckRemainderLowBasePair

## Role

This module completes the compatible pair-reduced second-order action on the
high and low Sobolev scales and returns the exact remainder to the complete
zero- and one-order path action.

## Verified state

Focused and exact verification pass with no `sorry`, `admit`, axiom
declaration, or `whnf`. The pair-reduced `A2` high/low bounds and commuting
square are complete. `remainder_path_split` is the exact algebraic base for
the remaining order-zero high-head extraction.

The final `A1` bounds `H3 -> H2`, `H2 -> H1`, and their compatibility remain
unstated and unproved (0%).

