# DeTurckRemainderLowBaseTimeA1

## Role

This module is the static first-order operator brick in the time-realization
lane.  It applies the canonical same-background C0+C1 two-jet estimate to
`lowCoreData` and then completes that exact action at the adjacent Sobolev
scales.

## Current state

`radialA1_pair` is stated on every already realized positive spectral `H2`
ball.  It uses no pairwise coefficient hypothesis and no high-state smallness.
The result names the canonical radial `lowCoreData`, bounds its `H3 → H2` and
`H2 → H1` first-order operators by one polynomial three-jet envelope, and
records their commuting square.

Focused and exact verification are GREEN against the refreshed time-module
export.  The temporary missing cutoff artifact was resolved upstream; no A1
declaration or proof obligation failed.  The static source frontier audit is
clean.

## Project accounting

- `ricci_flow_unif_existence`: unstated and unproved (0%).
- Dedicated uniform low-base machinery: about 98%.
- Static radial A1 operator realization: proved and exact-verified (100%).

## 2026-07-29 pair-transfer preparation

Private `radial_j2` and `radial_j3` convert the total radial-state differences
back to the exact smooth jet windows consumed by the forthcoming A1
two-state endpoint. The `H3` branch retains the required mixed
top-size-times-`H2`-difference arm and introduces no `H4` state dependence.
Focused verification is GREEN. The total A1 state-map continuity and
measurability theorem is still pending the final geometric pair estimate.
