# AppH2Hs

## Result

`appHs_h2_norm` upgrades the smooth three-dimensional `H2` product estimate
to the completed operator-field action on spectral `H2`.  Its constant depends
only on the fixed background metric and tensor ranks, while the coefficient
enters through its intrinsic order-zero-through-two covariant jet.

## Role

This is the passenger-completion interface for the low-regularity principal
operator.  The next producer must make the metric perturbation's first-slot
insertion a bounded linear map from spectral metric `H2` into the endomorphism
algebra of rank-four spectral `H2`; the Neumann inverse can then be taken in
that complete operator algebra.

## Verification

Focused verification passed.
