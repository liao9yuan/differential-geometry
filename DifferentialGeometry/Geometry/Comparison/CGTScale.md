# CGTScale

`exists_short_scale` is the canonical pure-real strict-budget lemma for CGT
core, fence, and curvature scales.  It was extracted from `CGTWhiteheadBigon`
so raw and intrinsic producers share one declaration without importing either
producer stack.

Source extraction is complete and focused verification passed without warnings.
The exported module is ready for a coordinated downstream refresh before its
consumers are checked.
