import DifferentialGeometry.Geometry.Comparison.CGTWhiteheadJensen

/-!
# Localized Whitehead producer for CGT

This stable umbrella preserves the `CGTWhitehead` import path while the
localized short-bigon proof, its true-distance producer, and the strict Jensen
consumer live in separate files below the 3000-line source limit.
-/
