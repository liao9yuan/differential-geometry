# Raw ball volume ratio

## Status

`rawBall_vol_rel` is not yet stated or proved: **0% theorem completion**.
The preceding compact-buffer raw polar equality is warning-free focused and
exact-refresh GREEN.  The remaining first lower producer is a general-α raw
radial density ratio: the checked `raw_ratio_anti` is limited to α = 0,
whereas the two-radius target consumes `hypRadVol q` for arbitrary `q ≥ 0`.

## Route

`raw_ratio_anti_q` is warning-free focused GREEN as the direct raw analogue of the existing
q=0 theorem.  It retains the honest raw-domain, speed, differential-injectivity,
orthonormal-transverse-frame, and radial Ricci-lower-bound inputs; it uses the
existing Jacobi/Riccati mean comparison and pole ratio, with model parameter
`q * a` for raw speed `a`.  No completeness, global curvature bound, or
consumer wrapper is added.

The first focused pass reached only the new file's import boundary: its lean
import did not bring the curvature, geodesic, covariant-along, and tangent
Jacobi names into the local namespaces, and the source-local orthonormal-frame
linear-independence helper is private upstream.  The file now imports the
native raw-density module explicitly, opens only those existing namespaces,
and contains the same small private frame-algebra fact; the radial proof route
and public inputs are unchanged.

The second focused pass then isolated two typeclass/coercion details only: that
private frame fact must omit the global compactness instance just as the
upstream local fact does, and the explicit smoothness annotation accidentally
introduced a doubly lifted order.  A focused retry further showed that this
file's ENNReal scope makes bare `∞` the wrong infinity; it now uses the same
explicit embedded manifold smoothness order as the raw polar module.  Both
repairs remain declaration-local.

After this producer checks, the next attempted assembly is `rawBall_vol_rel`:
combine `rawBall_integral_eq` at the two radii with radial integration on the
star-shaped raw interior locus.  A remaining failure must identify the smallest
missing raywise measurable/radial-disintegration interface rather than replace
the metric-ball equality by a one-sided volume bound.

The complete `SegmentPolar` route changes raw exponential density into a
normal-frame Euclidean polar integrand through a theorem that assumes global
completeness.  The raw analogue exists only as a private helper in
`RawMultiplicityArea`; this file now exposes the same lower-level measure
change as `rawJac_normal_int`, under only the raw-domain measurability it
actually needs.  It is warning-free focused GREEN.  Its first focused pass
found only the missing native measure namespace (`modelHaar`, `mapJacDensity`,
and `chartModelBasis`); opening that namespace preserved the theorem's inputs.

The checked normal-frame conversion is now followed by the first in-body
polar ingredient: the private `rawSegInt_down` establishes the radial
downward closure needed to pass a single raw interior endpoint to every
smaller positive radius.  It is a direct consequence of the extendible raw
definition and introduces no curvature or completeness premise.  Its first
focused pass found only that the raw-locus notation carries the native
`RiemannianBundle` instance, now stated explicitly on this private helper.
The second pass reduced its two proof obligations to scalar/unit-factor
normalization and nested scalar-action association; both are now rewritten
directly.  It is warning-free focused GREEN after omitting unused ambient
section instances.

The attempted `rawDn_contOn` polar ingredient was removed rather than leaving
a broken private declaration.  Its exact missing lower API is a public version
of `mapJac_contOn` on an open manifold map domain: the existing chart
continuity chain is private in `ManifoldImageLe`/`ManifoldImageEq`.  The same
gap blocks all three direct routes—normal-frame density, raw curve-density,
and `normalHaar_eq` measure transport.  The smallest next producer belongs in
that lower manifold-image layer; once it is exported, restricted
a.e.-measurability follows by `aemeasurable_indicator_iff` and the raw polar
Fubini assembly can resume.

The refreshed lower-layer `mapJac_contOn` is now available.  The endpoint
assembly now has a warning-free focused-checked private `raw_min_seg` bridge:
a raw minimizing endpoint, together with the local raw exponential domain,
gives exactly the arc-length minimality input required by `raw_ratio_anti_q`.
Its necessary `NeZero`, boundaryless, and tangent-Hausdorff instances are
native requirements of the radial-speed and metric-length APIs, not ambient
completeness assumptions.  The remaining endpoint work is the raywise
ratio-to-polar/Fubini assembly.

`raw_ratio_ray` is now warning-free focused-checked.  It consumes one raw
minimizing endpoint and produces the exact arbitrary-`q` antitone transverse
density ratio; the standard local domain, unit-speed, and no-conjugate inputs
are all discharged through `raw_min_seg` and the raw endpoint.  It retains
`SigmaCompactSpace` only because the existing `raw_exp_inj_of_min` API needs
it.  The theorem `rawBall_vol_rel` itself remains unstated and unproved.

The first normal-frame compact-buffer continuity pass has reached only a local
coordinate-domain mismatch: a point of the normal-frame preimage must be sent
through the frame before invoking the raw segment-domain lemma.  The producer
now records this as the explicit `L w ∈ expDomain` map-to-domain fact; no new
geometric assumption or alternate measure statement is involved.  A focused
retry is pending.

That retry isolated two local elaboration repairs: the raw-locus definition
must keep the file's coordinate-model normed-space instance rather than the
competing bundle-generated one, and the Jacobian-density equality needs its
common determinant factor canceled before applying `raw_exp_density`.  Both
repairs are source-local; the deferred next check is the first verification of
the normal-frame continuity bridge.

The boundary was then made generic over a set already mapped by the normal
frame into the exponential domain.  This is the actual lower continuity API:
it avoids imposing an incoherent equality between the coordinate-model and
tangent-bundle normed instances in a preimage statement.  Its focused check
elaborates; only three unused ambient section instances remain to be omitted
before warning-free verification.

The first compact-buffer normal-coordinate equality, `rawBall_normal`, is now
source-written.  It composes `rawBall_integral_eq`, pointwise
`raw_exp_density`, and `rawJac_normal_int`; it makes no set-equality claim and
does not assume ambient completeness.  Its focused verification is pending the
shared elaboration slot.

`rawBall_normal` is now warning-free focused GREEN.  Its deliberately generic
set parameter isolates the unavoidable coordinate-model/tangent-bundle
norm-instance boundary: the raw-interior-ball identity is supplied as an
equality for `K`, while the normal-frame preimage itself remains a plain set
operation.  This is a private implementation seam, not a new consumer-facing
assumption.  The next assembly directly adapts the checked polar and
two-radius integral comparison.

`rawBall_polar` is also warning-free focused GREEN: it combines that equality
with the raw strict-interior locus and polar disintegration, so each strict
metric ball is now an exact normal-frame radial integral.  The first full
source pass for `rawBall_vol_rel` is written but not yet verified.  It keeps
the Ricci lower bound on the compact buffer ball, chooses a ray endpoint
strictly before the buffer boundary, and invokes `raw_ratio_ray` only over
that locally controlled interval; no ambient completeness or global Ricci
bound is introduced.

## Accounting

- `rawBall_vol_rel`: source-written, unverified, **0% theorem completion**.
- General-α raw radial ratio: warning-free focused GREEN, **100% verified**.
- Normal-frame raw Jacobian conversion and radial down-closure: warning-free
  focused GREEN, **100% verified**.
- Dedicated P1b machinery: about **99%**; aggregate P1 endpoints: eleven of
  fourteen (**78.6%**); whole Poincare endpoint: unstated (**0%**).
