# CGTWhiteheadBigon

The pure-real `exists_short_scale` budget lemma now lives in `CGTScale.lean`.
This file imports that canonical declaration and retains the intrinsic Bigon
and regularity producers that consume it.

The import-only extraction is source-complete.  Focused verification is pending
the coordinated parallel-work window.
