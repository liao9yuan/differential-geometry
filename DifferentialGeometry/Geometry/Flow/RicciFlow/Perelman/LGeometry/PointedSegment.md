# Pointed restricted L-segment values

`lSegValue_limsup` combines the checked fixed-curve pointed action convergence
with the restricted-value competitor bound. Its eventual `IsLSegCurve`
hypothesis carries the transported-curve confinement explicitly, and its scalar
lower bound is restricted to those confinement sets. The source-side regular
and connected-space inputs are exactly the structural requirements of
`IsLSegCurve` and `lSegValue`.

Focused verification passed. This result does not assert finite-energy
competitor compactness, minimizer convergence, Hamilton--Harnack, or any
surgery/RFWS statement.
