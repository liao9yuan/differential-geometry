# UnifTopPathDev

## Status (2026-08-06)

`top_path_dev_unif` transports the class-first `phi_dev_h2_unif` estimate
through the canonical coefficient path integral.  Its radius and coefficient
are fixed from `gBase` and `Λ` before the order-three class metric varies.

Focused verification passed with four Lean threads under the 6 GB cap, without
warnings.  Its axiom audit reports only `propext`, `Classical.choice`, and
`Quot.sound`, so the integrated class-first deviation theorem is 100% complete
locally.  The fixed-curvature top arm, full joint tame producer,
`lowreg_bounds_unif`, and `ricci_flow_unif_existence` remain open; dedicated
supporting infrastructure is approximately 99%, and whole HCG closure remains
approximately 3%.
