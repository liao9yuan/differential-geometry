# ScratchUnifBoundsCensus.lean

This narrow census covers only the declarations in the explicit
common-horizon package.  It exists because the full ShortTime census crossed
the machine's physical-memory guard after loading the settled all-order lane.

Verification passed: every declaration prints only `propext`,
`Classical.choice`, and `Quot.sound`; none uses `sorryAx`.
