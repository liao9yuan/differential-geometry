# ScratchUnifRealizeCensus.lean

## Purpose

Narrow axiom census for the finite rank-two realization and zero-state package
chain.  It intentionally avoids importing the settled all-order campaign lane.

## Verification

The focused census passes.  Every listed realization, curvature-action, and
zero-state declaration reports exactly `propext`, `Classical.choice`, and
`Quot.sound`; no `sorryAx` enters this package chain.
