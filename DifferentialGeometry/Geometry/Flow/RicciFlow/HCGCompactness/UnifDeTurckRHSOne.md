# UnifDeTurckRHSOne

## Status

The intrinsic `j = 1` static Ricci--DeTurck bound is in progress.

The first new step is the radius-free reverse metric-jet bridge: the forward
class bound on `∇^{gBase} g₀`, together with metric comparability, controls
`∇^{g₀} gBase`.  Higher reverse jets will be obtained from the already proved
two- and three-step connection-change formulas.  This avoids introducing a
chart hierarchy and keeps the eventual Lie estimate intrinsic.

## Project position

`ricci_flow_unif_existence` itself remains unstated here and therefore remains
0% complete.  This module belongs to the dedicated E3/E6 forcing machinery.
