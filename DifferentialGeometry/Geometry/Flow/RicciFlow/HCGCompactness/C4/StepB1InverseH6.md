# H6 exact inverse chart tail

`H6NormalData.inv_chart_conv` extracts compact-open smooth convergence for the
exact local inverse of the H6 stage map. Every decode is restricted to
`NormalBallChart.restrictBall.target`, so no total-function inverse law outside
the chart image is assumed.

Focused and exact verification pass (`4153/4153`). The inverse chart brick is
complete.
