# H6 local metric control

`H6NormalData.source_stay` provides the buffered source-domain containment
needed by the provider-native metric component tower. Focused and exact
verification pass (`4145/4145`).

The local containment brick is complete and feeds both intrinsic norm tails.
