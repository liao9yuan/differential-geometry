# H6 reverse metric component tail

`H6NormalData.inv_cov_comp_tail` gives provider-native convergence,
smoothness, and coercivity for the exact inverse pullback metric component
tower. Focused and exact verification pass (`4190/4190`).

This reverse component brick is complete and feeds `inv_norm_tail`.
