# H6 metric bridge

`H6NormalData.chart_conv` and `pb_conv` express stage pullback convergence
through the same chart family `d.chart`. Focused and exact verification pass
(`4144/4144`).

This coordinate metric bridge is complete; no legacy fixed-base chart is used.
