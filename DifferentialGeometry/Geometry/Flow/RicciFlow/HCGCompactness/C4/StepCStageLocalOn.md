# Provider-native stage local diffeomorphism

`HasStageJetDataOn.hloc_tail` keeps one supplied `NormalChartFamily` through
the stage map and its jet readout. The H6 consumer calls it with `d.chart`.
Focused and exact verification pass (`4143/4143`).

This brick is complete. It feeds the H6 metric carrier; it is not an
unconditional compactness endpoint.
