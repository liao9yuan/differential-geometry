import DifferentialGeometry.Synthetic.Assembly

/-!
# Compatibility import for the synthetic assembly layer

The measure branch renamed the old `Synthetic.Axioms` module to
`Synthetic.Assembly`.  Keep this import-only module so existing files that
still import `DifferentialGeometry.Synthetic.Axioms` continue to elaborate.
-/
